<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
  <a class="navbar-brand" href="#">Index</a>
</nav>

<main class="container" role="main">
  <div class="jumbotron">
    <h1>¡Su cotización está lista!</h1>
    <div class="form-row">
        <div class="form-group col-md-4">
            <label for="monto">Costo de Financiamiento:</label>
            <input type="text" class="form-control" id="monto" name="monto" value="<%= number_to_currency(@costo, precision: 0, delimiter: ".") %>" disabled >
        </div>
        <div class="form-group col-md-4">
            <label for="folio">Giro a recibir:</label>
            <input type="text" class="form-control" id="folio" name="folio"value="<%= number_to_currency(@giro, precision: 0, delimiter: ".")%>" disabled >
        </div>
        <div class="form-group col-md-4">
            <label for="fechaVencimiento">Excedentes</label>
            <input type="text" class="form-control" id="folio" name="folio"value="<%= number_to_currency(@excedente, precision: 0, delimiter: ".") %>" disabled >
        </div>
    </div>
  </div>
  <div class="container"></div>
</main>
<br><br>
